plugins {
    id 'java-library'           // or just 'java' if you don't need 'api' configuration
}

java {
    withSourcesJar()
    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21
}

group = "${project.group}.core"
version = project.version
